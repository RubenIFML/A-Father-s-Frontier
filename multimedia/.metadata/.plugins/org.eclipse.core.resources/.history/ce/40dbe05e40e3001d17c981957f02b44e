package ow_elements;

import com.badlogic.gdx.scenes.scene2d.Stage;
import screens.OverWorldScreen;

public class Tile extends Element {
	private OverWorldScreen nivel;

	public Tile(float x, float y, Stage s, OverWorldScreen nivel, String tile) {
		super(x, y, s);
		this.nivel = nivel;
		
		loadFullAnimation(tile, 1, 1, 0.1f,false);
		this.setPolygon(8, 32, 23, 5, 5);
	}

	public void act(float delta) {
	    super.act(delta);
	    comprobarColisiones();
	}

	private void comprobarColisiones() {
		if (this.nivel.prota.overlaps(this)) {
			this.nivel.prota.preventOverlap(this);
		}
	}
}